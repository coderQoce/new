<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product</title>
</head>
<style>
            @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,400;0,500;0,600;1,700&display=swap');
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }
        .container{
            height: 100vh;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: black;
        }
        .form-login{
            background: #fff;
            border-radius: 6px;
            max-width: 430px;
            padding: 30px;
            width: 100%;
        }
        header{
            font-size: 30px;
            font-weight: 800;
            color: black;
            text-align: center;
        }
        .form-content label{
            font-size: 16px;
            font-weight: 400;
            color: black;
            text-align: center;  
        }
        form{
            margin-top: 30px;

        }
        form .input{
            height: 50px;
            width: 100%;
            margin-top: 30px;
            position: relative;
        }
        .input input,
        .input-btn{
            height: 100%;
            width: 100%;
            border: none;
            font-size: 16px;
            font-weight: 400;
            border-radius: 6px;
        }
        .input-btn{
            height: 50px;
            width: 100%;
            margin-top: 30px;
        }
        .input-btn:hover{
            background-color: black;
            color: #fff;
        }
        
        .input input{
            padding: 0 15px;
            border: 1px solid #cacaca;
        }
        .form-content a{
            text-decoration: none;
            color: #0171d3;
        }
        .form-content a:hover{
            text-decoration: underline;
        }
</style>
<body>
    <section class="container">
        <div class="form-login">
            <div class="form-content">
                <header>Add Products</header>
                <a href="/" class="hp">Home Page</a>

                <form action="/add" method="POST">
                    <div class="input">
                        <label for="name">Name:</label>
                        <input type="text" type="text" name="name" placeholder="Type text" class="input-content" required>
                    </div>

                    <div class="input">
                        <label for="type">Type</label>
                        <input type="text" id="type" name="type" placeholder="e.g shirt or trouser" class="input-content" required>
                    </div>

                    <div class="input">
                        <label for="qty">Quantity</label>
                        <input type="number" id="qty" name="type" placeholder="how many?" class="input-content" required>
                    </div>

                    <div >
                       <input type="submit" class="input-btn">
                    </div>
                </form>
            </div>
        </div>
    </section>
</div>
</body>
</html>